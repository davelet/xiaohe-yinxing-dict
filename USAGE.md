# 小鹤音形词典 · 使用说明

小鹤音形（flypy）输入法的**离线**桌面查询与管理工具。支持 macOS 和 Windows，词典数据内置在应用中，无需联网即可查询。

当前版本：**v2.2.0**

---

## 目录

- [安装与启动](#安装与启动)
- [界面概览](#界面概览)
- [词典查询](#词典查询)
- [输入法数据管理](#输入法数据管理)
- [AI 助手](#ai-助手)
- [帮助文档](#帮助文档)
- [键位图](#键位图)
- [自动更新](#自动更新)
- [macOS 原生菜单](#macos-原生菜单)
- [键盘快捷键一览](#键盘快捷键一览)
- [数据与配置文件位置](#数据与配置文件位置)
- [常见问题](#常见问题)

---

## 安装与启动

### macOS

1. 下载 `.dmg` 安装包，打开后将「小鹤音形词典」拖入「应用程序」文件夹。
2. 首次打开若提示「无法验证开发者」，请前往「系统设置 → 隐私与安全性」，点击「仍要打开」。
3. 启动后应用图标会出现在 Dock 中，菜单栏显示中文应用名。

### Windows

1. 下载 `.zip` 压缩包，解压到任意目录。
2. 双击 `xiaohe-yinxing-dict.exe` 运行。
3. 建议将快捷方式固定到开始菜单或任务栏。

> 应用窗口固定为 950×650 像素，不支持缩放和最大化。

---

## 界面概览

应用包含三个主视图，通过顶部工具栏切换：

| 视图 | 说明 | 进入方式 |
|------|------|----------|
| **词典** | 查询内置小鹤音形编码数据 | 默认视图 |
| **Rime 数据** | 管理本地 Rime 词典文件 | 点击 `→ Rime数据` 按钮 |
| **AI 助手** | AI 对话，解答小鹤音形问题 | 点击 `✨ AI 助手` 按钮 |

顶部工具栏从左到右依次为：

- 应用标题
- `⌨键位图` — 查看键位布局图
- `📖 帮助文档[F1]` — 打开内置帮助文档
- `❓关于` — 查看版本信息
- `✨ AI 助手[⌘,]` — 打开/关闭 AI 助手
- 分类筛选下拉框
- `→ Rime数据` 按钮（词典视图时）

底部状态栏显示搜索结果数量、词典总条目数、当前分类和版本号。

---

## 词典查询

### 搜索

在搜索框中输入内容即可实时查询：

- **输入编码**（小写字母，可含 `;`）：按编码反查字词，例如输入 `fu` 查找编码以 `fu` 开头的所有字词。
- **输入文字**（中文或其他字符）：按文字正查编码，例如输入「好」查找其编码。
- 搜索大小写不敏感。
- 最多显示 100 条结果，结果总数显示在底部状态栏。

搜索框支持以下操作：

- 点击搜索框右侧的 `×` 清空内容。
- 按 **Esc** 清空搜索词。
- 切换到词典视图时搜索框自动获得焦点。

### 搜索结果排序

结果按以下优先级排序：

1. 字数少的优先
2. 匹配精度：精确匹配 > 前缀匹配 > 子串匹配
3. 编码长度短的优先
4. 非次选字词优先

### 分类筛选

通过右上角的下拉框选择分类，可缩小查询范围。支持 16 种内置分类：

- 一级简码、二重简码、三码填空
- 四码全码（字）、四码全码（词）置顶、四码全码（词）
- 首选字词、快符、符号
- 部首部件、Emoji、微信表情
- 网站直达、随心所欲
- 二简（次选字）、四码（次选词）

也可以使用键盘 **↑ / ↓** 方向键快速循环切换分类。

右侧面板会显示当前分类的详细说明和示例。选择「全部」时显示小鹤音形整体介绍。

### 复制结果

搜索结果表格中有两列复制按钮：

- 点击 **📋文字** 复制字词到剪贴板
- 点击 **📋编码** 复制编码到剪贴板

点击后按钮会在 2 秒内显示 **✅文字** 或 **✅编码** 确认复制成功。

---

## 输入法数据管理

Rime 数据视图用于加载、搜索和管理本地 Rime 格式的词典文件（`.dict.yaml` / `.txt`），并支持直接添加新词到自定义词典。

### 进入视图

- 点击顶部工具栏的 `→ Rime数据` 按钮。
- 在词典视图按 **→** 方向键。
- macOS 菜单：`视图 → Rime 数据管理`（快捷键 `⌘⇧R`）。

视图使用淡紫色背景，与词典视图区分。按 **←** 方向键可返回词典视图。

### Rime 目录自动探测

应用启动时会自动探测 Rime 用户目录：

- **macOS**：`~/Library/Rime`
- **Windows**：`%APPDATA%\Rime` 或 `%LOCALAPPDATA%\Rime`

如果已保存的目录不存在，会自动重置为平台默认目录。

### 搜索外部词典

进入 Rime 数据视图后，在搜索框中输入文字或编码即可搜索所有已启用的外部词典。

- 最多显示 100 条结果。
- 结果中会显示每条词条来自哪个词典文件。
- 搜索结果支持按「文字」或「编码」列排序（点击表头切换升序/降序）。
- 同样支持一键复制文字和编码。

### 管理词典文件

搜索框为空时，显示词典文件管理区域：

- **🔄 重扫默认目录**：重新扫描 Rime 用户目录，发现新增的词典文件。
- **🗑 删除全部自定义词典**：删除所有手动添加的非 Rime 目录下的词典。
- **手动添加词典**：输入文件或目录的绝对路径，点击添加。输入目录会自动扫描其中所有 `.dict.yaml` 和 `.txt` 文件。

每个文件有三种状态：

| 状态 | 说明 | 可用操作 |
|------|------|----------|
| **已启用**（绿色） | 文件已加载，搜索时包含其内容 | 禁用、删除 |
| **已禁用**（灰色） | 文件未加载，搜索时不包含 | 启用、删除 |
| **未添加**（蓝色） | 自动扫描发现但尚未加入 | 添加 |

如果文件被移动或删除导致加载失败，顶部会显示红色警告，可一键移除所有失败项。

### 添加新词

1. 点击 **➕ 添加新词** 按钮，或按快捷键 **⌘N / Ctrl+N**。
2. 在弹出的对话框中输入：
   - **文字**：要添加的单字或词组
   - **编码**：对应的小鹤编码（小写字母 a–z）
3. 按 Enter 或点击「添加」。

应用会自动完成以下操作：

1. 将词条追加到 `flypy_custom.dict.yaml`（不存在则自动创建）。
2. 在 flypy 主词典中配置 `import_tables` 引用自定义词典。
3. 触发 Rime 重新部署（macOS 调用 Squirrel，Windows 调用小狼毫部署程序）。
4. 重新加载外部词典列表。

成功后会显示绿色提示，例如「已添加单字「好」(编码: hzk)，已自动部署」。

> **注意**：添加新词需要系统已安装 Rime 输入法（macOS 为鼠须管 Squirrel，Windows 为小狼毫 Weasel）。

### 自动重载

每次进入 Rime 数据视图时，应用会检查已启用词典文件是否有修改。如果检测到文件变更，会自动重新加载并提示。

---

## AI 助手

AI 助手可以解答小鹤音形相关问题，支持自然语言对话，并能调用内置工具查询词典数据。

### 打开 AI 助手

- 点击顶部 `✨ AI 助手` 按钮
- 按快捷键 **⌘, / Ctrl+,**
- macOS 菜单：`视图 → AI 助手`（快捷键 `⌘⇧A`）

AI 助手在主窗口内全屏显示，分为「AI 对话」和「AI 设置」两个标签页，可用 **← / →** 方向键切换。按 **↑** 或点击 `✖ 关闭` 退出。

### 首次使用

首次打开 AI 对话时会显示隐私声明确认框，请阅读后点击「我已了解，开始使用」。主要内容：

- 对话内容会发送到您选择的 AI 服务商。
- API Key 保存在本地配置文件中（明文存储）。
- 内置词库数据不会自动发送。
- 如果启用「外部词典工具」，AI 可能会将部分本地词库片段发送给服务商。

### 配置 AI 模型

在使用 AI 助手前，需要先添加至少一个模型：

1. 切换到「AI 设置」标签页。
2. 点击 **➕ 新增模型**。
3. 填写以下信息：

| 字段 | 说明 |
|------|------|
| 名称 | 自定义模型名称（必填，最多 50 字） |
| 提供商 | 选择 AI 服务商，或选择「自定义」 |
| API URL | API 接口地址（必填） |
| 模型 | 模型标识（必填） |
| API Key | 服务商提供的密钥（新增时必填，编辑时留空表示不修改） |
| 温度 | 控制回答随机性，0–200%，默认 70% |
| 词元上限 | 单次回复最大 token 数，默认 2048 |

支持的服务商包括：深度求索、智谱、月之暗面、通义千问、零一万物、字节豆包、腾讯混元、百度千帆、360智脑、海螺 MiniMax、百川、阶跃星辰、商汤日日新、面壁 MiniCPM、昆仑万维、出门问问、硅基流动，以及任意 OpenAI 兼容接口。

填写完成后可以点击 **🔌 测试连接** 验证配置是否正确。测试成功后点击「创建并激活」。

模型支持以下管理操作：

- **◎ 激活**：切换为当前使用的模型
- **✏ 编辑**：修改模型配置
- **🗑 删除**：删除模型（需二次确认）
- **📋 复制**：创建模型配置的副本

### AI 对话

在输入框中输入问题，按 **Enter** 发送，**Shift+Enter** 换行。

AI 回复支持 Markdown 渲染，流式输出逐字显示。生成过程中可以点击 **⏹ 停止** 中断回复。

每条 AI 回复下方提供快捷操作：

- **🔄 重新生成**：重新生成当前回复
- **🔍 词典中查看**：当 AI 调用过词典工具时可用，点击后跳转到主词典查看对应词条
- **📋 复制会话完整内容**：将整个对话导出为 Markdown 复制到剪贴板

工具栏功能：

- **📄 新建对话**：开始新的对话
- 显示当前激活的模型名称
- 如果开启了对话历史记录，可通过下拉框切换历史对话

### AI 工具

AI 助手可以调用以下工具来回答问题（对用户透明，AI 自动决定何时调用）：

- **查询文字编码**：根据汉字/词组查找小鹤编码
- **按编码反查**：根据编码查找对应字词
- **查询帮助文档**：获取帮助文档的指定章节
- **列出分类**：列出所有词典分类及条目数
- **分类统计**：查看分类的统计信息
- **搜索外部词典**（需手动开启）：搜索 Rime 外部词典数据

### 高级设置

在「AI 设置」页面底部：

- **启用外部词典工具**：开启后 AI 可搜索外部 Rime 词典，但可能将部分词库片段发送给 AI 服务商，默认关闭。
- **上下文保留轮数**：发送给 AI 的历史对话轮数（1–30，默认 10），值越大上下文越完整但消耗 token 越多。
- **最大工具调用轮次**：限制 AI 连续调用工具的次数（1–100，默认 10），防止无限循环。
- **记录对话历史**：开启后对话会保存到本地磁盘，工具栏会出现历史对话切换下拉框。
  - 可设置最大记录条数（1–1000，默认 50）
  - 可设置最大磁盘占用（100MB–20GB，默认 1GB）
  - 超出限制时自动删除最旧的对话
- **清空所有对话**：删除所有已保存的对话记录（需二次确认）。
- **打开对话目录**：在系统文件管理器中打开对话存储目录。

---

## 帮助文档

应用内置了完整的小鹤音形帮助文档，包含 16 个章节：

| 章节 | 内容 |
|------|------|
| 导读 | 帮助文档主索引 |
| 1 入门概述 | 小鹤音形整体介绍 |
| 1.1 双拼 | 双拼方案详解 |
| 1.2 双形（鹤形） | 鹤形概述 |
| 1.2.1 规则 | 鹤形拆分规则 |
| 1.2.2 字根 | 鹤形字根表 |
| 2 输入法应用 | 输入法应用概述 |
| 2.1 简码 | 简码字词表 |
| 2.2 符号编码 | 符号编码表 |
| 2.3 Win版指南 | Windows 版使用指南 |
| 2.4 安卓版指南 | 安卓版使用指南 |
| 2.5 挂接第三方 | 挂接第三方输入法 |
| 3 相关文章 | 相关文章 |
| 4 常见问题 | 常见问题解答 |
| 5 学习指引 | 学习路径指引 |
| 6 关于小鹤 | 关于小鹤音形 |

### 打开方式

- 点击顶部 `📖 帮助文档[F1]` 按钮
- 按 **F1** 键
- macOS 菜单：`帮助 → 打开帮助文档`

### 导航

- 左侧为章节列表，点击章节标题切换内容。
- 左侧顶部搜索框可搜索章节内容，输入关键词后会显示匹配的章节和摘要。
- 正文中的链接可点击跳转到对应章节。
- 搜索关键词在正文中会高亮显示。

### 退出帮助

- 点击左上角 `[←Del] 返回` 按钮
- 按 **Backspace** 退格键
- 再次按 **F1**

---

## 键位图

点击顶部 `⌨键位图` 按钮可以查看小鹤音形键位布局图。

- 图片以浮窗形式显示在按钮下方。
- 点击其他区域会自动关闭浮窗。

---

## 自动更新

应用启动时会在后台静默检查 GitHub 上是否有新版本。

### 更新流程

1. **发现新版本**：启动后如果检测到新版本，会弹出更新提示窗口，显示版本号和更新说明，可选择「立即更新」或「稍后再说」。
2. **自动下载**：选择更新后，应用会在后台自动下载安装包。屏幕底部显示下载进度条。
3. **SHA256 校验**：下载完成后自动校验文件完整性。
4. **安装**：校验通过后自动安装。
5. **重启**：安装完成后点击「🔄 立即重启」即可使用新版本。

您也可以在下载过程中：

- 点击「后台下载」隐藏进度条（下载继续进行）
- 点击「x 取消」取消下载
- 如果下载失败，点击「🔄 重试」重新下载

更新检查支持系统代理和 `HTTPS_PROXY` 环境变量。

---

## macOS 原生菜单

在 macOS 上，应用会创建原生菜单栏：

| 菜单 | 项目 | 快捷键 |
|------|------|--------|
| 小鹤音形词典 | 关于小鹤音形词典 | — |
| | 隐藏小鹤音形词典 | ⌘H |
| | 隐藏其他 | ⌘⌥H |
| | 全部显示 | — |
| | 退出小鹤音形词典 | ⌘Q |
| 视图 | AI 助手 | ⌘⇧A |
| | Rime 数据管理 | ⌘⇧R |
| 帮助 | 打开帮助文档 | F1 |

> Windows 没有原生菜单栏，所有功能通过应用内顶部按钮访问。

---

## 键盘快捷键一览

### 全局快捷键

| 快捷键 | 功能 |
|--------|------|
| F1 | 打开/关闭帮助文档 |
| ⌘, / Ctrl+, | 打开/关闭 AI 助手 |
| ⌘⇧A / Ctrl+Shift+A | 打开 AI 助手（macOS 菜单） |
| ⌘⇧R / Ctrl+Shift+R | 打开 Rime 数据管理（macOS 菜单） |

### 词典视图

| 快捷键 | 功能 |
|--------|------|
| ↑ | 切换到上一个分类 |
| ↓ | 切换到下一个分类 |
| → | 切换到 Rime 数据视图 |
| Esc | 清空搜索框 |

### Rime 数据视图

| 快捷键 | 功能 |
|--------|------|
| ← | 返回词典视图 |
| ⌘N / Ctrl+N | 添加新词 |
| Esc | 清空搜索框或关闭输入框 |

### AI 助手

| 快捷键 | 功能 |
|--------|------|
| ← / → | 切换对话/设置标签页 |
| ↑ | 关闭 AI 助手 |
| Enter | 发送消息 |
| Shift+Enter | 换行 |
| Esc | 关闭弹窗 |

### 帮助文档

| 快捷键 | 功能 |
|--------|------|
| Backspace | 返回应用 |
| F1 | 退出帮助 |
| Esc | 清空搜索框 |

### 对话框通用

| 快捷键 | 功能 |
|--------|------|
| Enter | 确认 |
| Esc | 取消/关闭 |

---

## 数据与配置文件位置

应用数据存储在系统配置目录下的 `xiaohe-yinxing-dict/` 文件夹中：

| 平台 | 路径 |
|------|------|
| macOS | `~/Library/Application Support/xiaohe-yinxing-dict/` |
| Windows | `%APPDATA%\xiaohe-yinxing-dict\` |

包含以下文件和目录：

| 文件 | 说明 |
|------|------|
| `config.json` | Rime 目录路径、外部词典列表等配置 |
| `ai_config.json` | AI 模型配置（包含 API Key，明文存储） |
| `conversations/` | AI 对话历史（需开启记录功能） |
| 外部词典缓存文件 | 加速外部词典加载的缓存 |

---

## 常见问题

### 应用完全离线吗？

内置词典查询、键位图、帮助文档完全离线。以下功能需要联网：

- AI 助手对话（需要连接您配置的 AI 服务商）
- 自动更新检查和下载

### 添加新词后没有生效？

请确认：

1. 系统已安装 Rime 输入法（macOS 鼠须管 / Windows 小狼毫）。
2. `flypy_custom.dict.yaml` 文件在 Rime 用户目录中。
3. 应用提示「已自动部署」成功。

### AI 助手无法连接？

1. 在「AI 设置」中点击「🔌 测试连接」检查配置。
2. 确认 API URL、模型名和 API Key 正确无误。
3. 检查网络连接和代理设置。
4. 确认所选服务商账户有可用余额。

### 如何备份配置？

备份配置目录下的 `config.json` 和 `ai_config.json` 文件即可。如需保留 AI 对话历史，同时备份 `conversations/` 目录。

### 搜索结果超过 100 条怎么办？

应用最多显示 100 条结果。请输入更精确的搜索词，或使用分类筛选缩小范围。

### 外部词典文件修改后需要手动重新加载吗？

不需要。每次切换到 Rime 数据视图时，应用会自动检测文件变更并重新加载。
